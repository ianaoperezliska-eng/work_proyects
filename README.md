# work_proyects
Before you try the program you need to install the packages that are in the requirements.txt
so you need to go to your terminal inside the route of the file that requirements.txt are and type (pip install -r .\requirements.txt)
with that now you can start usin the program without problems.
